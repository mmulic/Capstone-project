services:
  # ── React Frontend (Nginx) ───────────────────────────
  frontend:
    build:
      # Frontend source is now in the same repo under frontend/
      context: ./frontend
      dockerfile: ./deploy/frontend.Dockerfile
      args:
        # Empty string → axios uses relative URLs, nginx proxies /api/* to backend
        VITE_API_BASE_URL: ""
    container_name: disaster-frontend
    ports:
      - "80:80"
    depends_on:
      - app
    restart: unless-stopped

  # ── FastAPI Backend ──────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile  # production Dockerfile
    container_name: disaster-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  # ── PostgreSQL + PostGIS ─────────────────────────────
  db:
    image: postgis/postgis:16-3.4
    container_name: disaster-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_dev_password
      POSTGRES_DB: disaster_assessment
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init_db.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
